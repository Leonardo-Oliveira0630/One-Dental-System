
import React from 'react';
import { Navigate } from 'react-router-dom';

/**
 * Este arquivo foi substituído pela estrutura em /pages/admin/*
 * para melhorar a manutenção e isolamento de bugs.
 */
export const Admin = () => {
  return <Navigate to="/admin/setores" replace />;
};
